{
  "name": "booking-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prism:mock": "prism mock docs/openapi.yaml -p 8000",
    "prism:proxy": "prism proxy docs/openapi.yaml http://localhost:8000 --port 8080",
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "@stoplight/prism-cli": "^5.6.0"
  }
}
